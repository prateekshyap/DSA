  /*
https://practice.geeksforgeeks.org/problems/children-sum-parent/1/#
Children Sum Parent 
*/
  int isSumProperty(Node *root)
    {
        if(!root) return 1;
        
        int ret = treeKaRecursion(root);
       // cout<<" "<<isZero<<endl;
        if(isZero)
            return 0;
        // else if(ret == root->data) return 1;
        // else
        return 1;
    }